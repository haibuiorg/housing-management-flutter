import 'package:flutter/material.dart';

class CodeRegisterScreen extends StatelessWidget {
  const CodeRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
